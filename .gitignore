.env
.env.local
node_modules/
my-app/
dist/
.output
.tanstack/
.vite/
.cache
.tmp/
coverage/
playwright-report/
test-results/
playwright/.cache/
*.log
.DS_Store
# Optional legacy SQLite artifacts if present; Phase 01 uses PostgreSQL only.
data/*.db
data/*.db-*
# Local Postgres dumps (may contain repo metadata; do not commit).
data/pg-backups/*.dump
config/team-mapping.json
.obsidian/

# Local containerization — not part of the upstream project.
# Cover all four compose v2 override filenames so a user following modern
# compose docs (which prefer compose.*) doesn't accidentally commit local
# paths or secrets.
docker-compose.override.yml
docker-compose.override.yaml
compose.override.yml
compose.override.yaml